# libraries

import os
import shlex
import subprocess
from snakemake.utils import min_version

min_version("6.4.1")


##### version tracking #####

def find_workflow_directory():
    """
    Find the workflow directory using multiple heuristics.

    When Snakemake loads a Snakefile, __file__ may point to unexpected locations.
    This function tries several methods to find the actual workflow directory.

    Returns:
        tuple: (workflow_dir, project_root) where project_root is parent of workflow/
    """
    cwd = os.getcwd()

    # Method 1: Check current working directory for workflow/Snakefile
    snakefile_in_cwd = os.path.join(cwd, "workflow", "Snakefile")
    if os.path.exists(snakefile_in_cwd):
        return os.path.dirname(snakefile_in_cwd), cwd

    # Method 2: Check if we're already in workflow directory
    snakefile_here = os.path.join(cwd, "Snakefile")
    if os.path.exists(snakefile_here):
        return cwd, os.path.dirname(cwd)

    # Method 3: Walk up from cwd looking for workflow/Snakefile
    search_dir = cwd
    for _ in range(10):  # Max 10 levels up
        candidate = os.path.join(search_dir, "workflow", "Snakefile")
        if os.path.exists(candidate):
            return os.path.dirname(candidate), search_dir
        parent = os.path.dirname(search_dir)
        if parent == search_dir:  # Reached root
            break
        search_dir = parent

    # Method 4: Look for .snakedeploy file which indicates deployment root
    search_dir = cwd
    for _ in range(10):
        snakedeploy_file = os.path.join(search_dir, ".snakedeploy")
        if os.path.exists(snakedeploy_file):
            # Found deployment root, check for workflow/Snakefile
            candidate = os.path.join(search_dir, "workflow", "Snakefile")
            if os.path.exists(candidate):
                return os.path.dirname(candidate), search_dir
        parent = os.path.dirname(search_dir)
        if parent == search_dir:
            break
        search_dir = parent

    # Method 5: Look for .snakemake directory which indicates workflow root
    search_dir = cwd
    for _ in range(10):
        if os.path.exists(os.path.join(search_dir, ".snakemake")):
            candidate = os.path.join(search_dir, "workflow", "Snakefile")
            if os.path.exists(candidate):
                return os.path.dirname(candidate), search_dir
        parent = os.path.dirname(search_dir)
        if parent == search_dir:
            break
        search_dir = parent

    return None, None


def _read_version_file(candidate_dirs):
    """Return the trimmed contents of the first VERSION file found, else None."""
    for d in candidate_dirs:
        if not d:
            continue
        version_file = os.path.join(d, "VERSION")
        if os.path.exists(version_file):
            try:
                with open(version_file) as f:
                    text = f.read().strip()
                if text:
                    return text
            except OSError:
                pass
    return None


# Embedded workflow version. This is the SOURCE OF TRUTH for the version
# string and MUST be bumped in lockstep with the git tag at release time.
#
# Rationale: when the workflow is loaded via `module github(tag=...)`,
# Snakemake's source-cache only materialises the Snakefile + its explicit
# includes — sibling files like VERSION or .git are NOT available, so we
# cannot read them at runtime. The only file guaranteed to exist on the
# remote-loader's filesystem is this Snakefile itself, so the version has
# to live inline.
__WORKFLOW_VERSION__ = "v0.1.2"


def get_workflow_version():
    """
    Get workflow version for tracking.

    Resolution order (first hit wins):
    1. ``__WORKFLOW_VERSION__`` constant embedded above. Always set, always
       available regardless of how the Snakefile was loaded.
    2. VERSION file next to this Snakefile or at the repo root (only useful
       for local clones / archive tarballs; remote module loads can't see it).
    3. .snakedeploy metadata in cwd / project root — supports both legacy
       ``commit`` field and ``tag`` field.
    4. ``git rev-parse HEAD`` in the workflow / project / cwd tree
       (developer mode, with ``-dirty`` suffix when uncommitted).
    5. "unknown" fallback.
    """
    # 1. Embedded constant — set at release time, ships inside the Snakefile.
    if __WORKFLOW_VERSION__ and __WORKFLOW_VERSION__ != "unknown":
        return __WORKFLOW_VERSION__

    # Locate the workflow directory both via __file__ (works for local
    # clones) and via the legacy cwd-based heuristics. Remote module loads
    # via snakemake source-cache do NOT have sibling files available here.
    try:
        snakefile_dir = os.path.dirname(os.path.abspath(__file__))
    except NameError:
        snakefile_dir = None
    repo_root_from_file = os.path.dirname(snakefile_dir) if snakefile_dir else None

    workflow_dir, project_root = find_workflow_directory()

    # 2. VERSION file (release-time stamp on local clones / tarballs)
    version = _read_version_file(
        [snakefile_dir, repo_root_from_file, workflow_dir, project_root]
    )
    if version:
        return version

    # 2. .snakedeploy metadata (legacy `commit`, modern `tag`)
    import json
    for d in [project_root, os.getcwd()]:
        if not d:
            continue
        snakedeploy_file = os.path.join(d, ".snakedeploy")
        if os.path.exists(snakedeploy_file):
            try:
                with open(snakedeploy_file) as f:
                    data = json.load(f)
                for key in ("tag", "commit", "branch"):
                    if data.get(key):
                        return str(data[key])
            except (OSError, json.JSONDecodeError):
                pass

    # 3. git rev-parse (developer / git-clone mode)
    for search_dir in [snakefile_dir, repo_root_from_file, workflow_dir, project_root]:
        if not search_dir:
            continue
        try:
            result = subprocess.run(
                ["git", "rev-parse", "HEAD"],
                cwd=search_dir,
                capture_output=True,
                text=True,
                timeout=5,
            )
            if result.returncode == 0:
                commit_sha = result.stdout.strip()
                status_result = subprocess.run(
                    ["git", "status", "--porcelain"],
                    cwd=search_dir,
                    capture_output=True,
                    text=True,
                    timeout=5,
                )
                if status_result.returncode == 0 and status_result.stdout.strip():
                    return f"{commit_sha}-dirty"
                return commit_sha
        except (subprocess.TimeoutExpired, FileNotFoundError):
            pass

    return "unknown"


# Print version at workflow startup
WORKFLOW_VERSION = get_workflow_version()
print(f"\n{'='*60}")
print(f"NanoRNAMod Workflow Version: {WORKFLOW_VERSION}")
print(f"Started at: {__import__('datetime').datetime.now().isoformat()}")
print(f"{'='*60}\n")


wildcard_constraints:
    sample="[\da-zA-Z]+_?[0-9]*",
    control="[\da-zA-Z]+_?[0-9]*",
    native="[\da-zA-Z]+_?[0-9]*",
    sample_size="[0-9]+",
    n="[0-9]+",
    project="[\da-zA-Z]+_?[0-9]*",
    tool="[a-z0-9]+",


##### setup report #####
configfile: "config/config.yaml"


##### setup singularity #####


##### configurable tmp directory #####
# /tmp is often small and shared on HPC systems. Allow users to override the
# tmp directory via config (defaults to ./tmp in the working directory).
#
# This follows Snakemake's idiomatic tmpdir handling:
#   1. os.environ["TMPDIR"] is set so all subprocesses inherit it. Snakemake
#      itself uses $TMPDIR as the default for the special `tmpdir` resource.
#   2. We register it as a default resource so rules can refer to it via
#      `resources.tmpdir` and so it is tracked in benchmarks/reports. Users
#      can still override via `--default-resources tmpdir=/path` on the CLI.
_tmpdir = os.path.abspath(os.path.expanduser(config.get("tmpdir") or "tmp"))
os.makedirs(_tmpdir, exist_ok=True)
os.environ["TMPDIR"] = _tmpdir

# Only set the default if the user hasn't already provided one via the CLI
# (e.g. `--default-resources "tmpdir='/scratch/...'"`). Setting unconditionally
# would silently override the user's intent.
try:
    _existing = getattr(workflow.default_resources, "parsed", None) or {}
    if "tmpdir" not in _existing:
        workflow.default_resources.set_resource("tmpdir", _tmpdir)
except Exception:
    # Older Snakemake versions: env-var inheritance alone is enough.
    pass


##### raise file-descriptor limit for every shell rule #####
# The mkdir keeps the tmpdir present even if some other process cleans it
# mid-run. TMPDIR itself is inherited via os.environ above.
shell.prefix(
    "ulimit -n 65536 2>/dev/null || true; "
    f"mkdir -p {shlex.quote(_tmpdir)}; "
)


report: "report/workflow.rst"


include: "rules/common.smk"
include: "rules/prep_data_prep.smk"
include: "rules/prep_align_minimap2.smk"
include: "rules/prep_index_f5c.smk"
include: "rules/prep_eventalign_f5c.smk"
include: "rules/prep_reference.smk"
include: "rules/modetect_xpore.smk"
include: "rules/modetect_nanocompore.smk"
include: "rules/modetect_differr.smk"
include: "rules/modetect_drummer.smk"
include: "rules/modetect_eligos2.smk"
include: "rules/modetect_epinano.smk"
include: "rules/modetect_pybaleen.smk"
include: "rules/post_format.smk"
include: "rules/qc_quantification_nanocount.smk"
include: "rules/qc_coverage_qualimap.smk"
include: "rules/qc_fastq_nanoplot.smk"
include: "rules/qc_bam_samtools.smk"
include: "rules/polya_estimate_nanopolish.smk"
include: "rules/qc_variants_bcftools.smk"


##### target rules #####


rule all:
    input:
        get_final_output(),
        # "results/qc/multiqc_report.html",
        # "results/pca.svg",
